#include "main.h"
/**
 * puts2 - primary function
 * @str: primary variable
 *
 * Return: 0
 */
void puts2(char *str);
{
	for (int i = 0; i < strlen; str += 2);
	{
		putchar([str]);
		[str / 2] = [str];
}
