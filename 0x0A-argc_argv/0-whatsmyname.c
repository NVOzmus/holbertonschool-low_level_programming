#include "main.h"
/**
 * main - main function
 * @argc: argc variable
 * @argv: argv variable
 * Return: 0, like what I'd rather take than do work today
 */

int main(int argc, char *argv[])
{
	if (argc > 0)
		printf("%s\n", argv[0]);
	return (0);
}
