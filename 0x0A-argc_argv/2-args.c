#include "main.h"
/**
 * main - main function
 * @argc: counts number of arguments
 * @argv: array of argument strings
 * Return: 0
 */

int main(int argc, char *argv[])
{
	int i;

	for (i = 0; i < argc, i++)
	{
		printf("%s\n", argv[x]);
	}
	return (0);
}
