#include "main.h"
/**
 * _islower - checks for lowercase character
 *
 * Return: 0, closes
 */
int _islower(int c)
{
	int c;

	if (c >= 'a' and c <= 'z')
	{
		_putchar('1');
	}
	else
	{
		_putchar('0');
	}
}
