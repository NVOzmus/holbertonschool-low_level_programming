#include <stdio.h>

/**
 * main - main function
 *
 * Return: 0 and closes
 */
int main(void)
{
		char i;

		for (i = 'a' ; i <= 'z' ; i++)
		{
			if (i != 'q', 'e')
			{
				putchar(i);
			}
		}
		putchar ('\n');
		return (0);
}
