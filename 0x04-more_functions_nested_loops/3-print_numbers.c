#include "main."
/**
 * print_numbers - prints numbers
 *
 * Return: 0
 */

void print_numbers(void)
{
	char number;
	int count;

	for (count = '0'; count <= '9'; count++)
		_putchar(count);
	return (0);
}
